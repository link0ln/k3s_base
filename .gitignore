# Private SSH key — must not be committed
artifacts/id_ed25519

# kubeconfig contains cluster credentials and CA
artifacts/kubeconfig

# ArgoCD admin password
artifacts/argocd-admin-password

# Untrusted namespace kubeconfig
artifacts/untrusted-kubeconfig

# Ansible retry files
*.retry
